import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
    searchScreenContainer:{
        width: '100%',
        minHeight: '100%',
        alignItems: 'center',
        paddingTop: 60,
        
        
    }
})