import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
    componentContainer:{
        marginTop: 0,
        width: '100%',
        minHeight: '100%',

    },
    settingsTitle:{
        marginTop: 60,
        fontSize: 20,
        marginBottom: 20,
        marginLeft: '5%'
    }
})