import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
    componentContainer:{
        marginTop: 60,
        width: '90%',
        minHeight: '100%',
        marginLeft: '5%',
        
    },
    settingsTitle:{
        fontSize: 20,
        marginBottom: 20
    }
})